1、多次使用了LLM，会得到好几个结果。。后面好几段文字。。但是我只想用最后一个，怎么把前面的LLM的结果删掉，需要LLM流式的可以手动删除某个结果，删除后用户在生成新的，老的也不会重新出现
2、有用户反馈：Runninghub节点怎么从输出接进去的图可以成功，但是从上传接进去就是失败呀 这个是什么情况，但是我没有碰到这个情况，检查下，如果有问题就修复，没问题就记录回复即可，不要破坏其他功能
3、有时候文本中@不出来，没有弹出下来菜单，需要打一个空格才可以
4、一个问题一直都存在，当节点比较多的时候，框选范围漂移，会框选到很远的未知节点。。。
5、希望对着组右键，添加一个选项，可以直接删除组和组里的所有内容
6、即梦CLI，新增了图像模型seedream 4.7模型，这是即梦的文档：https://bytedance.larkoffice.com/wiki/FVTwwm0bGiishxkKOoScdHR2nsg，另外检查下即梦cli，还有没有遗漏的视频的seedance2.0的模型，用户说似乎还有，检查下，不确定，不要靠猜。有就加，没有就不加
7、提示词模板，生成的素材保存到提示词模板，需要可以i选择保存到分类，需要增加分类功能，分类可以删除，添加，重命名等
8、文本分割节点的输入框也要和其他的一样，有提示词模板，以及放大的按钮
9、现在再生成节点可以通过@素材的方式显示预览图，但是如果上游链接一个文本节点，没有办法在上游文本节点@图像，视频，音频等素材，需要也支持下
10、画布新增支持把生成的素材发送到figma
11、左下角添加一个"放置栏"，发送，粘贴的，都会篮子里有映射，默认处于折叠状态放置栏默认显示5个最近的素材，展开后可以显示最近20个最近的素材，用户可以直接拖动放置栏内的素材到画布，相关素材自动转移到用户拖动到的画布位置（注意是转移，不是新增）
12、用户生成好的素材，图像，视频，音频等，可以直接拖动到浏览器外的文件夹里
