# 贞贞的无限画布 v2.5.6

本版在保留 v2.5.5 Wan 2.7 Spicy 与 Dola Seedream 5.0 Pro 海外模型的基础上，完成画布 Agent / 工作流医生第 4 轮：接入运行中心权威证据、执行前只读体检和失败后诊断。

## 新增

- 工作流医生可引用持久化的 Run、NodeRun 与 Attempt，展示对应 Provider、模型、HTTP、requestId、轮询和规范化错误证据。
- 失败原因按平台、配置、网络和画布结构分类；缺少证据的字段保持未知，不从任意任务 ID、错误文本或历史记录猜测。
- 批量执行前新增只读体检，核对项目、画布、revision、节点范围、结构 digest、Provider 能力和可证实的成本信息；用户取消时不会调用 Provider。
- RunIntent 精确绑定 project / canvas / revision / run mode / target / requestId，幂等重试只接受完全等价内容，避免旧任务、错误目标或部分失败串写。
- reviewer 只获得解释与建议，editor 不能读取或修改主机 API Key；Agent 只看到当前已配置的能力，结构修复仍需预览和明确确认。

## 兼容

- v2.5.5 的 `wan-2.7-spicy-i2v`、`dola-seedream-5.0-pro-t2i` 与 `dola-seedream-5.0-pro-i2i` 保持不变。
- 旧画布继续默认国内 Seedream V5 Pro；未配置海外模型时不会自动切换到 Dola。
- 没有明确价格证据时，执行前体检只显示成本未知，不猜 Provider 调用次数或价格。

## 验证

- E4 聚焦前端 364/364、后端 160/160；全量 211 个测试文件、1655/1655 通过（TS 1302，CJS 353）。
- `npm run type-check -- --pretty false`、`npm run public:check`、`npm run rh-toolbox:check` 与 `git diff --check` 通过；公开构建 2088 modules，正式 Electron / 私有构建 2090 modules。
- 真实浏览器验证执行前体检与失败后诊断：诊断引用具体 Run / NodeRun / Attempt，并基于 HTTP 504、`ETIMEDOUT`、`providerCalled=false` 将测试失败归类为网络问题；浏览器控制台 error 为 0。

## Electron 与自动更新

- 字节码编译固定使用 `package-lock.json` 锁定的 Electron 33.4.11；系统 Node、错误 Electron 版本或不完整的本地 Electron 会直接阻断打包，避免生成 V8 不兼容安装包。
- 正式 Windows x64 NSIS 构建、后端加密、运行时归档和 post-build 全部通过；`win-unpacked` 包含 93 个 `.t8c`、0 个明文后端 JS，EXE ProductVersion 为 2.5.6.0、FileVersion 为 2.5.6。
- 同一隔离 userData 连续冷启动两次均返回 v2.5.6，并在用户已有 2.5.5 / 18766 后端旁正确选择 18767；两次均正常关闭、端口释放，日志无 fatal、`cachedDataRejected` 或进程崩溃。
- SQLite schema 22、迁移 1-22 连续，`quick_check=ok`、`foreign_key_check=0`；RunIntent、Run、NodeRun 与 Attempt 证据表和关键列齐全。
- 自动更新交付包含 `T8-PenguinCanvas-Setup-2.5.6.exe`、对应 blockmap 与 `latest.yml`；v2.5.6 Release 为非草稿、非预发布并标记为 Latest。
- `v2.5.6` tag 与发布源提交同指 `affdaa07c04262746a4b65af96af7835d8d6744e`，`main` 随后仅快进追加本发布记录；远端三项资产的 size 与 SHA-256 digest 均和本地实物一致。

> 安装包可覆盖旧版本；应用内“检查更新”会读取本 Release 的 `latest.yml`。
